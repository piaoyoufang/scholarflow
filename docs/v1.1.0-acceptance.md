# ScholarFlow v1.1.0 验收记录

验收日期：2026-08-03

验收分支：`feature/v1.1.0-upgrade`

## 自动化验收

- [x] 完整 pytest 测试 `21/21` 通过。
- [x] 离线发布门禁全部通过。
- [x] 混合检索和 rerank 单元测试 `3/3`。
- [x] 长期摘要记忆离线测试 `3/3`。
- [x] 诊断 Agent 离线测试 `3/3`。
- [x] 上传 API 离线测试 `6/6`。
- [x] 真实阿里 Chat 与 Embedding 连接成功，向量维度为 1024。
- [x] 阿里 `gte-rerank-v2` 真实调用成功，无失败、重试或降级。
- [x] 通用真实评估 `20/20`。
- [x] MCP 与诊断专项真实评估 `8/8`。

## 在线链路验收

- [x] 鉴权 Markdown 上传成功并写入 Chroma。
- [x] 上传资料经 `knowledge_agent` 检索并生成真实引用。
- [x] 首次问题生成会话标题，线程历史为 2 条。
- [x] 在线验收产生的临时线程、会话、文件和向量已清理。
- [x] 真实摘要模型保留项目目录和“先给结论”的用户偏好。

## Docker 验收

- [x] `scholarflow-api-1` 状态为 `healthy`。
- [x] `scholarflow-web-1` 状态为 `Up`，健康页面返回 HTTP 200。
- [x] 容器内 rerank 模型为 `gte-rerank-v2`，地址为完整百炼原生端点。
- [x] 容器 API 完成上传、问答、引用、标题和清理的完整在线链路。
- [x] Web 页面返回 HTTP 200，容器在线验收脚本全部通过。
- [x] 重启前后线程数保持 34、checkpoint 数保持 105、向量片段数保持 7。
- [x] API 与 Web 日志没有 traceback 或未解释的 4xx/5xx。

## 结论

ScholarFlow v1.1.0 的四项升级已完成：检索效果、记忆系统、Agent 能力和前端产品化均通过离线、真实模型及 Docker 验收，可以发布。
